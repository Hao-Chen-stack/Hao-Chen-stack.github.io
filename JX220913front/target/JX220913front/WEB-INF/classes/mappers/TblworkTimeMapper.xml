<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--通过namespace属性和自定义的接口关联-->
<mapper namespace="com.cykj.mapper.TblworkTimeMapper">

    <select id="findWorkTimeMsg" resultType="com.cykj.bean.Tblworktime">
        select * from tblworktime
        where managerId = (select managerId from tblmanager where managerAcc = #{managerAcc})
        and workDate = #{workDate}
    </select>

    <select id="findWorkTimeMsgCount" resultType="java.lang.Integer">
        select count(*) from tblworktime
        where managerId = (select managerId from tblmanager where managerAcc = #{managerAcc})
        and workDate = #{workDate}
    </select>
</mapper>